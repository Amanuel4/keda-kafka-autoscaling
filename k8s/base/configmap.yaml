apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-config
data:
  KAFKA_BOOTSTRAP: demo-kafka-cluster-kafka-bootstrap.kafka.svc.cluster.local:9092
  KAFKA_TOPIC: notification-events
  KAFKA_GROUP: notification-processors